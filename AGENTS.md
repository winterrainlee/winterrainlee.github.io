---
created: 2026-04-27
updated: 2026-04-27
---

# AGENTS.md

이 프로젝트는 Astro 기반 개인 블로그다. 작업할 때는 공개 저장소와 개인 운영 정보가 함께 다뤄질 수 있음을 항상 고려한다.

## 기본 원칙

- 구현 전에 요청의 성공 조건을 짧게 정한다. 여러 해석이 가능하거나 전제가 불확실하면 먼저 확인한다.
- 더 단순한 접근이 있으면 말하고, 요청 범위를 넘어서는 기능은 추가하지 않는다.
- 기존 디자인 톤을 유지한다: 얇은 라인, 낮은 대비의 반투명 배경, 8px radius, 조용한 타이포그래피.
- 불필요한 리팩터링을 피하고 요청 범위에 가까운 파일만 수정한다.
- 새 기능은 정적 GitHub Pages 환경에서 동작하는지 확인한다.
- Supabase 관련 기능은 브라우저에 secret을 노출하지 않는다.
- 실제 secret 값, webhook URL, service role key, 관리자 비밀번호는 절대 커밋하지 않는다.

## 변경 방식

- 최소 코드로 요청을 해결한다. 단일 사용처를 위한 추상화나 요청받지 않은 설정 옵션을 만들지 않는다.
- 기존 코드 스타일을 따른다. 다르게 쓰고 싶어도 주변 코드와 일관성을 우선한다.
- 인접 코드, 주석, 포맷을 임의로 개선하지 않는다.
- 내가 만든 변경 때문에 생긴 미사용 import, 변수, 함수는 정리한다.
- 기존에 있던 무관한 죽은 코드는 삭제하지 말고 필요하면 사용자에게 알린다.
- 모든 변경 줄은 사용자의 요청이나 그 요청을 검증하기 위한 작업으로 설명 가능해야 한다.

## 실행과 검증

- 여러 단계 작업은 간단한 계획과 각 단계의 검증 방법을 세운다.
- 버그 수정은 가능하면 재현 방법을 먼저 확인하고, 수정 후 같은 경로로 검증한다.
- 새 기능은 빌드와 관련 사용자 흐름을 확인한다.
- 사소한 변경도 최소한 `npm run build`를 기본 검증으로 사용한다.

## 보안 문서 기준

- 공개 가능한 운영 원칙은 `docs/security_guide.md`에 둔다.
- 민감한 운영 runbook은 `docs/private/`에 둔다.
- `docs/private/`는 Git에서 추적하지 않는다.
- Supabase project ref, 관리자 경로, 보호 헤더 이름, 함수별 테스트 명령처럼 운영 단서를 자세히 담은 문서는 공개하지 않는다.
- `docs/` 아래 공개 문서를 만들거나 수정할 때는 작업 마지막에 프론트매터의 `created`, `updated` 날짜를 확인하고 갱신한다.

## 방명록 구조

- 공개 방명록 페이지는 `src/pages/guestbook.astro`다.
- 관리자 답글 페이지는 `src/pages/admin/guestbook.astro`다.
- Supabase Edge Functions는 `supabase/functions/` 아래에 둔다.
- 비밀글 본문과 답글은 공개 목록에서 노출하지 않는다.
- 비밀글 답글은 작성자가 비밀번호를 입력했을 때만 보여준다.
- 관리자 작업은 Edge Function을 통해 수행하고, service role key는 Edge Function 환경에서만 사용한다.

## 개발 명령

변경 후 기본 확인:

```sh
npm run build
```

로컬 확인:

```sh
npm run dev -- --host 127.0.0.1
```

## Git 주의사항

- 사용자 변경을 되돌리지 않는다.
- 커밋 전 `git status --short`로 의도치 않은 파일을 확인한다.
- `dist`, `.env`, `docs/private`, `supabase/.temp`는 커밋하지 않는다.
- 운영 문서를 공개 위치에서 제거할 때도 로컬 사본이 필요한지 먼저 확인한다.
